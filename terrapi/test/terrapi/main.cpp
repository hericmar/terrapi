#include "fmt/format.h"

#include "tests.h"

int main()
{
    test_config();

    /*
    auto& app = App::Get();
    app.Initialize(fmt::format("{}/example/configuration.ini", TERRAPI_PROJECT_ROOT));
    app.Run();
     */
}
